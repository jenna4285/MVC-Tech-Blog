<section class="container">
  <h3 class="blog-name">{{blog.title}}</h3>
  <h4>Posted: {{format_time blog.date_created}}</h4>
  <section class="flex-row justify-center">
    {{#each blog.title as |title|}}
    {{> allblogs-details}}
    {{/each}}
  </section>
</section>